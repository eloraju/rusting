name: test-and-document
on: 
  pull_request:
    branches:
      - master

jobs:
  test:
    name: Run tests
    runs_on: ubuntu-latest
    container: rust:latest
    steps:
      - uses: actions/checkout@v2
      - run: cargo test
