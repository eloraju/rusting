name: test
on: 
  pull_request:
    branches:
      - master

jobs:
  test:
    name: Run tests
    runsmon: ubuntu-latest
    container: rust:latest
    steps:
      - uses: actions/checkout@v2
      - run: cargo test
